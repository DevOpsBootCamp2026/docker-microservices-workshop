# USERS-SERVICE Dockerfile — YOU WRITE THIS (Node/Express app: server.js).
#   TODO 1: a Node base image (small)
#   TODO 2: set a working directory
#   TODO 3: copy ONLY the dependency manifests first  ← why? (layer caching)
#   TODO 4: install production dependencies
#   TODO 5: copy the rest of the code
#   TODO 6: document the port (see PORT -> 3000)
#   TODO 7: the start command
