# TASKS-SERVICE Dockerfile — YOU WRITE THIS (Python/Flask app: app.py).
#   TODO 1: a Python base image (small)
#   TODO 2: set a working directory
#   TODO 3: copy ONLY requirements.txt first  ← why first? (layer caching)
#   TODO 4: install the dependencies
#   TODO 5: copy the rest of the code
#   TODO 6: document the port the app listens on (see app.run -> 5000)
#   TODO 7: the command that starts the app
