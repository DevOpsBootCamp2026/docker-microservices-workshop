# GATEWAY Dockerfile — YOU WRITE THIS.
# Goal: an Nginx image that (a) serves the static frontend and (b) loads the
# provided nginx.conf so it can reverse-proxy /api/* to the services.
#   TODO 1: base image that runs Nginx
#   TODO 2: install the provided nginx.conf as the site config
#   TODO 3: copy index.html, app.js, style.css into the web root
#   TODO 4: document the port
#   TODO ?: does this base image already start Nginx? do you need a CMD?
